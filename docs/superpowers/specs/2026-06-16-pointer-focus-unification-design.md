# Design: unify pointer + focus hit-testing on one authority

Date: 2026-06-16. Branch: `revert-34-host-first-hittest` (the #34 revert is the
base; this fix lands on top, single PR). HW target: silence (GhostBSD/Linux +
cinnamon + wezterm/gkrellm).

## Problem

Cinnamon sloppy mouse-focus is unreliable on yserver: hovering a window often
fails to focus it (worst on wezterm). The root cause is **two pointer
hit-tests over two trees, consulted by different subsystems**, which disagree
on the Composite Overlay Window (`0x103`) and cinnamon's stage (`0xb00011`):

| Subsystem | Hit-test | Store | keying |
|---|---|---|---|
| Click `target` (`pointer_fanout.rs:249`) | core `root_pointer_target_at` | `ServerState.shape_windows` | `ResourceId` |
| Crossings/focus (`backend.rs:5171` `dispatch_motion_event`) | backend `window_under_cursor` | `KmsCore.shape_input` | host `u32` |

### Confirmed divergence (evidence)

The backend store cannot represent an **empty input region**.
`mirror_shape_to_host_state` → `backend.set_shape_rectangles` does
`if rects.is_empty() { dst.remove(host) }` (`backend.rs:15045`). An absent
entry makes `cursor_inside_shape` return `true` (opaque). The core store keeps
`input = Some([])`, which `window_input_contains` correctly treats as
click-through.

From the captured yserver xtrace (`cinnamon.xtrace`): cinnamon creates region
`0x00f00011` **empty** (`rectangles=;`) and applies it to *both* the stage and
the COW input shapes — the normal "pass clicks through to apps" state. Result:

- **Core**: COW/stage = click-through (correct). Clicks (`target`) work.
- **Backend**: COW/stage = opaque (wrong). `window_under_cursor` returns the
  COW/stage subtree, so the `Enter` generated by `dispatch_motion_event` lands
  on cinnamon's stage instead of the app frame. muffin never sees the `Enter`
  on the app → **sloppy focus silently fails**.

This is precisely the revert-branch symptom ("clicks work, focus unreliable").
It also explains why both prior fixes failed: **#34** (clicks host-first) and
**`fix/unified-pointer-sprite`** (focus host-first) each pushed an authority
toward the *backend*. Xorg uses a single sprite trace over its one window
tree for both pointer and keyboard; the correct direction is the opposite —
everything resolves through the **core resource tree**.

## Goal

One input-shape store, one hit-test — the core resource-tree miSpriteTrace
walk that already honors `None`(opaque) / empty(click-through) — used for
clicks, crossings, motion, and PointerRoot key focus. The backend's parallel
input authority is removed.

Non-goal (separate, pre-existing): recomputing the sprite on map/restack under
a stationary pointer (Xorg's `WindowsRestructured` path; see
`project_yserver_cursor_focus_bugs`). Out of scope for this PR.

## Design

The core hit-test (`ServerState::root_pointer_target_at` → the
`hit_test_children` / `window_input_contains` walk over `resources` +
`shape_windows`) becomes the **single** pointer authority. `target` (clicks)
already uses it on this branch; we route the crossing/motion/cursor path
through it too and delete the backend's competing authority.

Two prerequisites surfaced in review must land *before* the backend authority
is removed, or they become regressions.

### 0. Teach the core hit-test the bounding-shape fallback (prerequisite)

Xorg's sprite trace gates a hit on the bounding shape AND the input shape as
**two independent checks** (`mi/miwindow.c:767`, mirrored in
`dix/window.c`):

```
(!wBoundingShape(pWin) || PointInBorderSize(pWin, x, y))
    && (!wInputShape(pWin) || RegionContainsPoint(wInputShape(pWin), ...))
```

i.e. a point hits iff it is inside the bounding shape (when one is set) **and**
inside the input shape (when one is set). The backend's `cursor_inside_shape`
approximates this as `shape_input.or_else(shape_bounding)` — close but not
exact: when *both* are set it tests only the input region. The core
`window_input_contains` (`server.rs:1916`) is further off — it checks **only**
the input shape and treats "no input shape" as the full window rect, ignoring
the bounding shape entirely. So a window with a bounding-only shape is hittable
in its clipped-away corners under the core walk (already wrong for clicks
today; removing the backend walk extends it to crossings, motion,
`QueryPointer`, and `XIQueryPointer`, which all route through `direct_child_at`).

Fix `window_input_contains` to the Xorg conjunction over `ShapeWindowState`
(`server.rs:1465`): a point hits iff `(bounding unset || in bounding) && (input
unset || in input)`. This preserves the empty-input-vs-no-shape distinction
(`input = Some([])` → input set, point not in empty set → click-through;
`input = None` → input gate passes) and is strictly Xorg-faithful, not just
behavior-identical to the backend walk it replaces. Border-width expansion of
the bounding region (`PointInBorderSize` uses `borderSize`) is out of scope —
yserver hit-tests at the rect level; note it as a follow-up if a bordered
shaped window misbehaves on HW.

### 1. Route crossings + motion + cursor through the core hit-test in ResourceId space

The pointer window is resolved once per motion as a **`ResourceId`** via
`state.root_pointer_target_at(cursor_x, cursor_y)` and threaded through the
crossing/cursor path in `ResourceId` space, deriving `host_xid` only at wire
emit. This avoids a lossy `ResourceId → host_xid → ResourceId` round-trip: a
mapped child whose `host_xid` is momentarily missing/stale
(`register_subwindow` can register a window before parent/geometry catch up,
and `update_pointer_window` has an "unmapped host_xid" fallback) would
otherwise silently collapse to ancestor/root semantics and corrupt crossings,
motion coords, and cursor selection.

Concretely:

- Change `KmsCore.prev_pointer_window` from `Option<u32>` (host) to
  `Option<ResourceId>` (5 call sites: `core.rs:1538`, `backend.rs:1243`,
  `5093/5100/5161`, `5303`).
- `dispatch_motion_event` (`backend.rs:5167`) and `process_pointer_button`
  (`backend.rs:5237`) resolve the target `ResourceId` from
  `root_pointer_target_at` (cursor in root/fb coords, same space the deleted
  backend hit-test used), replacing `window_under_cursor()`.
- `update_pointer_window` takes a `ResourceId`, compares against the
  `ResourceId` `prev_pointer_window`, calls `normal_mode_crossings`
  (already `ResourceId`-based), and derives `host_xid` only inside the
  `emit_crossing` loop (the existing `w.host_xid` lookup at
  `backend.rs:5128`). `emit_motion_only` likewise derives `host_xid` at emit.
- `refresh_effective_cursor` (`backend.rs:1243`) walks the cursor chain from
  the `ResourceId` pointer window. `effective_cursor_walking_chain`
  (`backend.rs:1219`) currently walks `windows_v2` host ancestry — migrate it
  to walk the core resource parent chain (cursor attributes), or the
  `ResourceId` starting key is fixed while the chain stays host-stale.
- **No residual host-space islands.** Two more consumers must move off
  `host_xid` or the emit step re-collapses to host space: `event_relative_coords`
  (`backend.rs:5028`) derives event coords from `host_xid` — compute them from
  the resolved `ResourceId`'s core geometry instead; and the `emit_crossing`
  fallback that rewrites a missing endpoint host XID to `new_xid`
  (`backend.rs:5128`) must not silently collapse — when a crossing-endpoint
  window has no `host_xid`, emit from core geometry rather than substituting
  another window. The implicit-grab path in `process_pointer_button`
  (`backend.rs:5300`) must reuse the already-resolved target `ResourceId`
  rather than re-deriving focus/grab through `xid_map`.

### 2. Delete the backend input authority

Once nothing reads it:

- Remove `window_under_cursor` (`backend.rs:4910`) and `cursor_inside_shape`
  (`backend.rs:5004`) plus their unit tests (port their intent — empty input
  region, bounding-only fallback, deepest-child descent — onto the core walk).
- Remove `KmsCore.shape_input` (`core.rs:1574`), its initializers, and the
  `kind == 2` arm of `set_shape_rectangles` (`backend.rs:15039`).
- Drop `KIND_INPUT` from `mirror_shape_to_host_state` (the mirror existed only
  to feed the backend hit-test). Continue mirroring `Bounding`/`Clip` — those
  feed the renderer (`scene.rs:2395`, `backend.rs:2327`) and stay.

`ServerState.shape_windows` is then the only input-shape store.

### Scope boundary

- **KMS v2 backend only.** The `host_x11`/ynest backend gets `host_xid` from
  the real host X server (which does its own correct hit-test and honors empty
  regions via the host's SHAPE/XFIXES), so its path is already single-authority
  against the host. Do not touch it.

## Risks

- **Bounding-fallback behavior change.** Step 0 changes hit-testing for
  bounding-only shaped windows (they stop being hittable outside the bounding
  region). This is the Xorg-correct behavior and matches the deleted backend
  walk, but it is a behavior change for clicks too — cover with a test and
  watch shaped clients (oclock/xeyes-style, decorations) on HW.
- **Lingering host-side pointer state.** `prev_pointer_window` and the cursor
  chain are the residual host-space state that could re-create the prior
  "cursor/focus says ancestor, click says child" failure if left stale —
  especially for override-redirect sub-windows and unmap/teardown races. Step 1
  threads `ResourceId` end-to-end specifically to close this; verify the
  cursor and crossings on OR popups and on destroy/unmap of the prev pointer
  window.
- **Non-obvious consumers of the input path.** Two prior fixes built green but
  failed on HW (#34: focus≠click; unified-pointer-sprite: no clicks). Both
  pushed an authority toward the *backend*; this change is the *inverse*
  (core-first, matching Xorg). The lesson holds regardless: **HW observation
  gates the commit** — no merge before silence confirms.
- **Coordinate space.** `cursor_x/cursor_y` (backend, fb/root `f32`) must match
  `root_pointer_target_at`'s expected `i16` root coords. Verify with an edge
  test at non-zero window and child offsets, plus the existing trace.

## Testing

- **Core hit-test unit tests** (`window_input_contains` / `hit_test_children`):
  - empty input region on a parent → click-through (pointer descends to the
    sibling beneath), mirroring the cinnamon COW/stage case.
  - bounding-only shape (no input shape) → hittable inside, excluded outside
    the bounding region (the Step 0 fallback).
  - **both** bounding and input set, with the input region extending partially
    *outside* the bounding region → hits only where both contain the point
    (the case that distinguishes the Xorg conjunction from `input.or_else`).
  - `None` input + `None` bounding → full-rect opaque.
  - deepest-child descent and non-zero window/child offset coordinate cases
    (port the intent of the deleted `window_under_cursor_*` tests).
- **Protocol path tests:** `QueryPointer` / `XIQueryPointer` `child`
  resolution under empty input region and bounding-only shape; active-grab
  press/release where the focus/natural target differs from the grab window
  (implicit-grab crossings still correct).
- **Mapping edge cases:** core hit resolves a window whose `host_xid` is
  `None`; COW lifecycle (materialize/release) transitions; root-window hit;
  destroy/unmap of the previous pointer window mid-stream.
- **Host-gap emission:** resolved target is correct in core but its `host_xid`
  is missing — assert motion/crossing/button event coords and target are still
  correct (derived from core geometry), not silently collapsed to root or the
  destination window. Without this the `ResourceId` threading can look correct
  while emit-time host fallback reintroduces the old failure class.
- **Build/clippy/test:** `cargo +nightly fmt`, `cargo clippy`, `cargo test`
  (core + yserver) all green.
- **HW (gating):** on silence, cinnamon + wezterm and gkrellm. Confirm sloppy
  focus follows the pointer reliably (wezterm especially), clicks land on the
  hovered window, and gkrellm buttons respond. Capture a yserver trace
  (`...pointer=trace,...pointer_fanout=debug`); the matching Xorg behavior is
  the reference.

## Key files

- `crates/yserver/src/kms/v2/backend.rs` — `dispatch_motion_event` (5167),
  `process_pointer_button` (5237), `window_under_cursor` (4910),
  `cursor_inside_shape` (5004), `set_shape_rectangles` (15027),
  `update_pointer_window` (5092).
- `crates/yserver/src/kms/core.rs` — `shape_input` (1574).
- `crates/yserver-core/src/server.rs` — `root_pointer_target_at` (1827),
  `window_input_contains` (1916), `shape_windows`.
- `crates/yserver-core/src/core_loop/process_request.rs` —
  `mirror_shape_to_host_state` (422), `SET_WINDOW_SHAPE_REGION` (4072).
- `crates/yserver-core/src/crossings.rs` — `normal_mode_crossings` (90).
- Reference: `../xserver/mi/miwindow.c` (miSpriteTrace),
  `../xserver/dix/events.c` (CheckMotion, DeliverFocusedEvent).
- Background: handoff `docs/superpowers/notes/2026-06-16-pointer-focus-unification-handoff.md`;
  memory `project_sloppy_focus_dual_hittest`.
